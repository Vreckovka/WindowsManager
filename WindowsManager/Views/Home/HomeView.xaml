<UserControl x:Class="WindowsManager.Views.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WindowsManager.Views.Home"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">

    <UserControl.Resources>
        <ResourceDictionary Source="HomeViewResourceDictionary.xaml" />
    </UserControl.Resources>


    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding RargbtTorrrents}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <controls:VirtualizingWrapPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="350"
                                Margin="5"
                                Padding="7"
                                Background="#151515"
                                BorderBrush="#45cccccc"
                                BorderThickness="1">
                            <Grid DataContext="{Binding Model}">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid VerticalAlignment="Top">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>


                                    <StackPanel Grid.Column="0" Margin="0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Margin="0,0,7,5"
                                                       FontSize="27"
                                                       FontWeight="Bold"
                                                       Foreground="#e8b761"
                                                       Text="{Binding SeedersOrderIndex}" />

                                            <Image Width="35"
                                                   Height="35"
                                                   HorizontalAlignment="Left"
                                                   VerticalAlignment="Center"
                                                   Source="{Binding CategoryImagePath}" />
                                        </StackPanel>



                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource ParameterLabel}" Text="Created:  " />
                                            <TextBlock FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="#ccc"
                                                       Text="{Binding Created, StringFormat={}{0:MM.dd.yy}}" />
                                        </StackPanel>


                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource ParameterLabel}" Text="Size:  " />
                                            <TextBlock FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource YellowBrush}">
                                                <Run Text="{Binding Size}" />
                                                <Run Text="{Binding SizeUnit}" />
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource ParameterLabel}" Text="Seeders:  " />
                                            <TextBlock FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="{StaticResource GreenBrush3}"
                                                       Text="{Binding Seeders}" />
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource ParameterLabel}" Text="Leechers:  " />
                                            <TextBlock FontSize="15"
                                                       FontWeight="Bold"
                                                       Foreground="#959595"
                                                       Text="{Binding Leechers}" />
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Style="{StaticResource ParameterLabel}" Text="Link:  " />
                                            <Button Background="Transparent"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.OpenInBrowser, RelativeSource={RelativeSource AncestorType=Border, Mode=FindAncestor}}"
                                                    Content="{Binding Href}"
                                                    Cursor="Hand"
                                                    FontSize="15"
                                                    FontWeight="Bold"
                                                    ToolTip="{Binding Link}">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type Button}">
                                                                    <Border Background="{TemplateBinding Background}">
                                                                        <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}" />
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="Foreground" Value="#7b994e" />
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="true">
                                                                <Setter Property="Foreground" Value="#c2eb86" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </StackPanel>

                                        <ContentControl Content="{Binding DataContext, RelativeSource={RelativeSource AncestorType=Border, Mode=FindAncestor}}" ContentTemplateSelector="{StaticResource TorrentTypeTemplateSelector}" />

                                    </StackPanel>

                                    <Image Grid.Column="1"
                                           Width="80"
                                           Height="120"
                                           HorizontalAlignment="Left"
                                           Source="{Binding ImagePath}" />


                                </Grid>

                                <TextBlock Grid.Row="1"
                                           Margin="0,10"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center"
                                           FontSize="15"
                                           FontWeight="Bold"
                                           Text="{Binding Name}"
                                           ToolTip="{Binding Name}" />

                            </Grid>
                        </Border>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>
    </Grid>
</UserControl>
